# Empty in development — requests go to /api and the Vite proxy forwards them
# to the backend on port 3001. For a deployed backend set this to the API base
# URL, e.g. https://abc123.execute-api.us-east-1.amazonaws.com
VITE_API_BASE_URL=
